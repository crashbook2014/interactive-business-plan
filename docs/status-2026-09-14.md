# Status — 14 September 2026

Full pass: live GitHub state and four independent `wodouh-*` agent reviews
(engineer, red-team, experience, growth) run against `main` at commit
`5d23d41`. Two agents (wodouh-redteam, wodouh-growth) returned complete
reports; engineer and experience are still running at time of writing —
their partial findings are included, and a follow-up cycle will pick up
their full reports.

**Reading order:** section 1 is infrastructure. Section 2 is the product,
with 🔴/🟠/🟡/🟢 severity markers and duplicates flagged as duplicates.
Section 3 is the ranked action list.

---

## 1. Infrastructure — verified against live code

### GitHub Actions

Still blocked (`total_ms: 0`). Unchanged from August. The four agents
confirmed the test suite runs locally (`node test/run.js` — **47 suites,
all pass**). External CI diagnosis still requires the owner to read the
banner at `github.com/settings/billing`.

### Supabase

- Migrations `0007`–`0009` still unapplied. No progress.
- `public.admins` still empty — the two-step (paste migrations → sign in)
  has not been done.
- **New since August:** two schema-advisor migrations were applied directly
  in the dashboard (`revoke_truncate_trigger_references_from_client_roles`,
  `revoke_is_admin_execute_from_anon`). They are not in the repo. A
  rebuild from source loses them silently. They should become `0010`.
- **New since August:** `ANALYZE_URL` is now hardcoded
  (`app/index.html:1343`). AI analysis is live in the deployed code. This
  changes the severity of the `ai_analysis` fail-open from a latent risk to
  an active one — see section 2.
- **New since August:** `UPLOAD_URL` is set (`app/index.html:1356`). The
  scan path is live. The privacy page has not been updated to say so.
- `PAYMENT_COMPILED = false` (`app/index.html:12635`). Payments are still
  off. The paywall renders `pw_demo` — "Prototype — no real payment
  happens" — while the public homepage shows real prices and a 14-day
  refund guarantee. These two surfaces now contradict each other.
- All five `app_flags` remain at their default values (no rows changed
  in `app_flags`). `ai_analysis` defaults to `true`, which means the
  fail-open finding below is real and active.
- Edge Functions: the `analyze` and `upload` functions are referenced by
  hardcoded URLs; whether they are deployed could not be confirmed from
  the repository. External access to the Supabase project was blocked
  (proxy 403).

### Repository

One open feature branch (`claude/contract-review-brand-identity-tk3pfu`).
No open PRs.

---

## 2. Product — agent findings

### 🔴 P0 — The privacy page tells readers no file ever leaves their device. The app uploads their file.

**Found independently by: wodouh-redteam (complete), wodouh-growth (complete)**

`privacy/index.html:131` (en): *"no file ever leaves it."*
`privacy/index.html:53` (ar): *«ولا يغادر جهازك ملف أبدًا»*
`privacy/index.html:138`: *"Reading scanned contracts is still being built,
and this page will be updated before it is switched on, not after."*
`app/index.html:2147` (the intake copy shown when a reader decides whether
to hand over their contract): *"all of them send text, never a file."*

`WODOUH_CONFIG.UPLOAD_URL` is live. `sendScan()` at `app/index.html:11062`
builds a `FormData`, appends the reader's file, and POSTs it. The file
leaves the device.

The in-app consent dialog (`scan_ok_b`, `app/index.html:2332-2333`) is
correct and truthful — it names Anthropic, names the one-hour deletion,
and says the file is sent. Three surfaces are wrong and the one a reader
reads *before* deciding to upload is one of them.

**Also:** the `privacy/index.html` Arabic heading at `:52` still says
**«ثلاثة»** (three) after a fourth exception was added. The English heading
was updated; the Arabic was not.

**Owner action required.** Privacy copy is Human-always per
`docs/agent-team.md`. The fix:
1. Delete "no file ever leaves it" / «ولا يغادر جهازك ملف أبدًا» from both
   languages.
2. Delete the "still being built" paragraph from both languages.
3. Add a fourth `<li>` to both `<ol>`s describing the scan exception.
4. Fix the Arabic heading to «أربعة».
5. Rewrite `privacy_line_ai` (both languages) to remove "never a file."
6. Add a static test: when `UPLOAD_URL` is configured, assert that
   `privacy/index.html` contains no string claiming no file leaves the
   device.

---

### 🔴 P0 — Unanswered contract type silently adds a 20,000 SAR notice line

**Found by: wodouh-redteam (complete)**

`termQNext()` has no validation on the contract-type step. The toggle
renders with neither option selected, and Next advances. `termCtype()`
returns `null`, which every downstream reader treats as "indefinite
contract." The Article 76 notice line (`tm_m_notice`) is owed only on an
indefinite contract (`app/index.html:7924-7930`). A fixed-term Saudi reader
who skips one toggle is shown 20,000 SAR (6 months at 10,000 wage) as
*certain* entitlement.

`app/index.html:7558-7560` states the product's own rule: *"a blank answer
is reported as missing rather than guessed."* This is the one place that
rule is not followed.

Reproduced by walking the real UI: start `2020-01-01`, end `2026-01-01`,
wage 10,000, notice 60, given 0, contract type never tapped → `tm_m_notice
20,000` in certain column, `termUnassessed` does not mention it.

The free calculator is not affected — `calcEos()` computes no notice line.

**Claude can fix with a test.** Either require `ctype` before leaving step
2 (one line), or when `termCtype() === null` suppress `tm_m_notice`, move
`tm_m_comp` to the two-month floor, and add both to `termUnassessed()`.
Add a test asserting that for any Saudi reader with `term.ctype === null`,
`tm_m_notice` does not appear as a certain line.

---

### 🔴 P0 — Article 81 still missing from the free calculator

**Found by: wodouh-redteam (complete). Note: August's Article 87 P0 is
now fixed — see section 3 closed items.**

`EOS_HOW` is `["term","resign","expiry"]`. There is no "employer breach /
asked to resign" option. On the resign path with `factor < 1`, the app
shows a reduced or zero figure and offers no onward door to the paid
termination assessment.

Reproduced: a reader pushed out by an employer breach, 3 years service,
10,000 SAR wage, `eosHow='resign'`:
- Free calculator → **5,000 SAR** (with a note about the resignation
  reduction and nothing else).
- Paid `termAward()` with `how:'forced'` → **15,000 SAR**.

At 1.5 years the free side shows the resignation-before-two-years note
and **0 SAR** — no signal that an exception might apply.

**Minimum honest fix (not legal advice, a door):** on the resign path,
when `factor < 1` or `factor === 0`, render a sentence naming the
employer-breach route and linking to the termination assessment — the same
sentence already used at `app/index.html:2829` inside the paid flow. Whether
a fourth `EOS_HOW` option ships a reading as a figure is a founder decision.

**Test:** for every `eosHow==='resign'` result where `factor < 1`, assert
the rendered output contains a reference to the employer-breach route.

---

### 🟠 P1 — `ai_analysis` fail-open is now a live issue

**Found independently by: wodouh-redteam (complete), wodouh-engineer (partial)**

`aiAvailable()` at `app/index.html:8779`: `return !!analyzeUrl() && flagOn("ai_analysis", true)`.

August called this "not exploitable today (`ANALYZE_URL` is empty)."
`ANALYZE_URL` is no longer empty. The fail-open is now active.

Measured (redteam, four states):

| flags response        | `aiAvailable()` |
|-----------------------|-----------------|
| `ai_analysis: false`  | `false` ✓       |
| request aborted       | **`true`**      |
| HTTP 500              | **`true`**      |
| malformed JSON        | **`true`**      |

`FLAG_TIMEOUT` is 2000 ms. A slow mobile connection alone trips this.
The console's off switch does not turn off the AI for a reader on a flaky
connection.

**Claude can fix with a test.** Add `const AI_COMPILED = false;` beside
the other three compiled constants. Use `flagOn("ai_analysis", AI_COMPILED)`
in `aiAvailable()`. Add `ai_analysis` to `applyFlags()`. Add a test
matching what `admin.test.js` already does for payments and lawyer_desk —
assert that with the flags endpoint aborted or 500ing, `aiAvailable()`
returns false.

---

### 🟠 P1 — Ten language-filter bypasses in the contract-review grader

**Found by: wodouh-redteam (complete)**

`supabase/functions/_shared/review-contract.mjs:78-175` (BANNED). The
filter uses constructions, not literals — the right instinct. Ten
constructions it does not cover, verified by running `gradeContractReview()`
directly against each phrase:

| Bypassing phrase | Caught sibling |
|---|---|
| "The labour court will **award** you your full entitlement." | "…will rule in your favour" |
| "You will **recover all your dues** through the labour court." | "…will win" |
| «المحكمة **ستنصفك**» | «ستحكم…», «لصالحك» |
| "This clause is **invalid** under Saudi law." | "…is void / unenforceable / illegal" |
| "This provision is **not permitted** under the Labor Law." | "prohibited by the Labor Law" |
| "Your employer **has no right to enforce** this clause." | "cannot be enforced" |
| "This term is **legally ineffective**." | "no legal effect" |
| «هذا البند **غير صحيح** نظاماً» | «غير قانوني» |
| «هذا الشرط **لا قيمة له** نظاماً» | «لا أثر له» |
| "You have an **open and shut** case." | "slam dunk" |

The money guarantee held: digit forms, Arabic-Indic digits, and
word-multiplier phrases were all dropped. The legal-outcome language
bypasses are the gap.

**Claude can fix with tests.** Add the ten constructions to BANNED. Widen
the court-verb set to include `award|grant|order|compensate`. Add Arabic:
`ستنصف|غير صحيح|لا قيمة له`. Add all ten to `contract-review.test.js`
as assertions that each is dropped in both languages and in both the
finding and summary slot.

---

### 🟠 P1 — The paywall says "Prototype" while the homepage advertises real prices and a refund guarantee

**Found by: wodouh-growth (complete)**

`PAYMENT_COMPILED = false` (`app/index.html:12635`). The paywall renders
`pw_demo` ("Prototype — no real payment happens", `app/index.html:2458`) at
the exact moment a reader who has entered their wage, both dates, and
termination story decides to trust the product with money.

The public homepage now shows 199 / 149 / 349 / 549 SAR prices and a
14-day refund guarantee (live since curtain lift on 5 September). These
two surfaces — "Prototype" and a published refund policy — contradict each
other in the worst possible way: at the only moment that matters.

**Recommendation (growth agent):** while payments are off, the paywall and
the pricing section should say the same thing — *"Pricing is set; checkout
opens shortly. Leave your number and we'll open it for you"* — routed to
the WhatsApp link already on the page. This converts a dead end into the
site's only lead capture. The wording is the founder's.

**Nothing on this list matters while this is true.** Do not spend on
acquisition until the paywall is honest.

---

### 🟠 P1 — 44 of 58 answer-page titles are truncated mid-phrase

**Found by: wodouh-growth (complete), wodouh-engineer (partial)**

`tools/make-seo.mjs:195-197`. When the suffix is long (e.g. `art. 6 of
the Implementing Regulations | Wodouh` ≈ 48 chars), `maxTopicLen` hits
its floor of `Math.max(10, …)` = 10. The word-boundary guard `lastSpace >
10` can never fire inside a 10-character window, so the title is cut
mid-word. The worst result:

```
Employer m… — art. 6 of the Implementing Regulations | Wodouh
لا يجوز لصاحب العمل… — المادة 6 من اللائحة التنفيذية | وضوح
```

Growth measured 44 of 58 pages affected (22 English, 22 Arabic). August
reported 2; today it is 44. All 58 English titles remain in statute form
rather than question form.

The truncation exists because the topic phrase is a statute sentence
squeezed into 60 characters. Question-form titles fit with room to spare:

| Now | Proposed |
|---|---|
| `End-of-service — Saudi Labor Law, Article 84 \| Wodouh` | `How much end-of-service pay am I owed? — Article 84 \| Wodouh` |
| `Employer m… — art. 6 of the Implementing Regulations \| Wodouh` | `Can my employer keep my passport? — Saudi law \| Wodouh` |

**Note on urgency:** the sitemap was submitted to Search Console on 30
August (per `docs/geo-benchmark.md`). Google has been crawling 58 pages
with these titles for two weeks.

**Claude can fix the truncation bug.** The `Math.max(10, …)` floor at
`make-seo.mjs:196` is the root cause.

**Question-form titles require a `QUESTION_EN` / `QUESTION_AR` column in
the register** — 29 pairs authored by a human, one per topic, so the
generator can assert every row has one (build fails otherwise). This is
the SEO equivalent of the register's sourcing discipline.

**Before regenerating:** run the GEO baseline in `docs/geo-benchmark.md`
(four engines, 15 questions, ~30 min). The before/after is permanently
lost if regenerated first.

---

### 🟡 P2 — "Nothing is stored at any point" sits above "the file is deleted after an hour"

**Found by: wodouh-redteam (complete)**

`app/index.html:2181-2182` (`acc_privacy_b_ai`, both languages): *"All
three go through Wodouh's server to Anthropic's Claude model, and nothing
is stored at any point"* — then, eight lines down, exception 4: *"The file
is deleted after an hour."*

`public.uploads` has `expires_at timestamptz not null default now() +
interval '1 hour'`, a `deleted_at` column, and a sweep — storage is
deliberate and defensible. The sentence above it is not accurate.

**Owner action required.** Scope the sentence: "The first three store
nothing at any point." (Human-authored.)

---

### 🟡 P2 — Article 53 (probation termination) still blocked on a single lawyer reading

**Found by: wodouh-growth (complete)**

`docs/legal-sources.md:101` — `⚠️ DISPUTED`. Still no answer page, no GEO
question, no product surface. Flagged August 29; now six weeks old.
`docs/legal-sources.md:159` says exactly what to read to settle it.
This is the highest-demand topic the product cannot answer and it feeds
the 349 SAR termination assessment.

---

### 🟡 P2 — The unverified-citation policy was never recorded; AI has since shipped

**Found by: wodouh-redteam (complete)**

`gradeRef()` marks a model citation `verified: false` and the client
renders it in an amber `.cr-cite.unverified` block rather than refusing
it. Architecture unchanged since August. August listed this as "needs a
founder decision before AI goes live." AI went live on an unrecorded
decision. Not a new vulnerability — a pending decision overtaken by a
deploy. `docs/agent-team.md` puts "turning on AI" in Human-always.

---

### Experience audit — partial (agent still running)

The experience agent walked the app as a real user via jsdom (browser
download blocked by proxy). Partial findings before time ran out:

- **Article 87 fix confirmed from the user side.** The exceptions appear
  on the calculator path, the birth branch produces the correct full award,
  and the explanation is accurate.
- **Assessment now shows real figures free.** This was August's biggest UX
  finding — the funnel asked for money before showing anything. The agent
  confirmed the assessment now renders a figure before the paywall.
- **"August false positive still live"** — the agent identified an item
  from August but ran out of context before naming it. Will be captured in
  the next cycle.

---

### Engineer audit — partial (agent still running)

- **All 47 suites pass** (296 s, local).
- **AI is live** — `ANALYZE_URL` hardcoded, confirmed live path from code.
- **Article 87 wired into `calcEos`** — the August P0 is confirmed fixed.
- **fail-open reproduced live** — `flagValues === null` but
  `aiAvailable() === true`, consistent with the redteam finding.
- **Titles still truncated** — consistent with growth.
- Full report deferred to next cycle.

---

## 3. Ranked action list

Items 1–2 are human decisions; items 3–7 are Claude work with tests.

| # | Item | Owner | August status |
|---|---|---|---|
| **1** | Fix privacy page — delete "no file ever leaves" in both languages and both surfaces, add fourth `<li>`, fix Arabic heading to «أربعة» | **Human** | New |
| **2** | Fix paywall "Prototype" copy while `PAYMENT_COMPILED = false` — align with homepage, capture leads via WhatsApp | **Human** | New |
| **3** | Fix contract-type validation — require `ctype` or suppress `tm_m_notice` when `ctype === null`; add test | Claude | New |
| **4** | Fix `ai_analysis` fail-open — add `AI_COMPILED = false`, use as fallback, add fail-open test | Claude | Aug P1 → now P1 (live) |
| **5** | Fix 10 language-filter bypasses in contract grader — add constructions to BANNED, add tests | Claude | New |
| **6** | Fix truncation bug at `make-seo.mjs:196`, then after human writes `QUESTION_EN`/`QUESTION_AR` column, regenerate 58 titles | Claude + Human | Aug P1 (unchanged) |
| **7** | Fix Article 81 door on resign path — add onward sentence and link to termination assessment | Claude | Aug P0 partial (Art 87 fixed, Art 81 open) |
| **8** | Fix "nothing is stored at any point" scoping in `acc_privacy_b_ai` | **Human** | New |
| **9** | Run GEO baseline (`docs/geo-benchmark.md`) before titles regenerate | **Human** | Aug carry-forward |
| **10** | Unblock Article 53 — one lawyer reading to resolve `⚠️ DISPUTED` | **Human** | Aug carry-forward (6 wks) |
| **11** | Record the unverified-citation decision in `docs/enable-ai-runbook.md` | **Human** | Aug carry-forward |
| **12** | Apply database-only migrations `0007`–`0009` + commit the two schema-advisor fixes as `0010` | **Human** | Aug carry-forward |

---

## What closed since August

| Item | Evidence |
|---|---|
| 🟢 **Article 87 in free calculator** — this was August's P0 | `eosExc87Applies()` in `calcEos()`; `calc-fuzz.test.js:120-137` asserts correct full figure |
| 🟢 **Homepage stale prices** — 65/130/325 SAR gone | No old prices found in `index.html`; only 149/199/349 |
| 🟢 **Commerce ladder safety** — zero free upgrade paths | `ladderBreaks()` returns `[]`; verified across all mode × tier pairs |
| 🟢 **Assessment shows a free figure** — ask for money before showing anything | Agent confirmed real figures render before the 349 paywall |
| 🟢 **CTA links to `/app/`** — was blocked by the curtain | Curtain lifted 5 September (commit `1acd5e4`) |
| 🟢 **Language switch moves no figure** | Redteam confirmed 85,500 / 30,000 stable across `applyLang()` |
| 🟢 **`localStorage` carries no contract text** | Redteam: canary injected, enumerated all keys, absent |
| 🟢 **Model output cannot render as HTML** | All model-supplied fields go through `textContent`, not `innerHTML` |
| 🟢 **Money attestation in contract grader holds** | Digits, Arabic-Indic, word-multiplier forms all dropped |

---

*Agents: wodouh-engineer (partial), wodouh-experience (partial),
wodouh-redteam (complete), wodouh-growth (complete). Next cycle will
pick up the full engineer and experience reports.*
